# 虚拟环境
venv/
.venv/
env/

# 运行日志 / 临时探测脚本
logs/
*.log
._*body_*.txt
_probe_*.py
run_v*.log

# 运行时状态（推送记录、信号日志等）
data/pushed_signals.json
data/pushed_legs.json
data/notifications.jsonl

# 本地配置（含本机设置，不提交；请用 config.example.yaml）
config.yaml
config.local.yaml

# Python 缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 密钥 / 环境变量
.env
*.key
*.pem

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# 代理内部记忆（非产品文件）
.workbuddy/

# 本机启动器（Windows 任务计划程序调用，不提交）
start_service.bat
start_service.ps1

# W1 模块：运行时历史快照（每台机器独立积累，不提交）
data/w1_rnd_history.json

# 临时探测脚本 / research 产物（本地验证用，不提交）
w1_ftest.py
w1_research_synthesize.png

# 本机停止器（与 start_service 同性质，含本机路径，不提交）
stop_service.bat
stop_service.ps1
